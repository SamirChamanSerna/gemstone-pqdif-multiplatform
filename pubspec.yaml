name: gemstone_pqdif_multiplatform
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^0.5.1
  flutter_riverpod: ^2.5.1
  file_picker: ^8.0.7
  protobuf: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
